# The torch-free serving stack (Phase 7, D-034): the Docker image and the Gradio Space.
# Versions equal those in uv.lock (tests/test_deploy.py checks it).
fastapi==0.141.1
numpy==2.5.3
onnxruntime==1.30.0
pydantic==2.13.5
tokenizers==0.23.2
uvicorn==0.54.0
